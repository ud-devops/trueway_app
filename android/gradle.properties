org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Djava.io.tmpdir=C:\\T
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# Must stay false on this toolchain: shared_preferences_android 2.4.23 applies
# `kotlin-android` itself, which AGP 9 rejects when built-in Kotlin is on
# ("The 'org.jetbrains.kotlin.android' plugin is no longer required"). The fixed
# version (2.4.27) needs a newer Flutter than this project targets.
#
# The Kotlin Gradle Plugin is therefore applied explicitly to every Android
# module in build.gradle.kts — see the note there.
android.builtInKotlin=false
